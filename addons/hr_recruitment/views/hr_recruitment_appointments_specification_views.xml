<?xml version="1.0"?>
<odoo>
    <!--    Appointment Specifications Page Action-->
    <record model="ir.actions.act_window" id="action_hr_appointments_specification">
        <field name="name">Appointment Specification</field>
        <field name="res_model">hr.appointments.specification</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" eval="False"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No specifications yet!
            </p>
            <p>
                Odoo helps you track applications in the recruitment
                process and follow up all operations: meetings, interviews, etc.
            </p>
            <p>
            </p>
        </field>
    </record>
</odoo>
